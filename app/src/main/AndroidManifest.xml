<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.onpressure.projeto.onpressure">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.CadastroActivity"
            android:label="SobPressão"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".Activities.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.PerfilActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".Activities.CalculoImcActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".Activities.PressaoArterialActivity"
            android:label="@string/app_name" />
        />
        <activity
            android:name=".Activities.MedicamentosIndicadosActivity"
            android:label="@string/app_name" />
        <![CDATA[
        />
        />
        ]]>
        <activity
            android:name=".Activities.LembretesActivity"
            android:label="@string/app_name" />
        />
        <activity
            android:name=".Activities.SintomasActivity"
            android:label="@string/app_name" />
        />
        <activity
            android:name=".Activities.AlimentosActivity"
            android:label="@string/app_name" />
        />
        <activity
            android:name=".Activities.AberturaActivity"
            android:label="@string/app_name"
            android:theme="@style/AppCompat.TelaCheia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        />
        <activity
            android:name=".Activities.GraficosActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".Activities.HistoricoActivity"
            android:label="@string/app_name" />
        <![CDATA[
        />
        />
    ]]>
        <activity android:name=".Activities.TabelaSodioActivity"></activity>
    </application>

</manifest>